version: '3.4'

services:

  web:
    environment:
      - Mailgun__Domain=TODO
      - Mailgun__ApiKey=TODO
    volumes:
      - ./TechCalendar.Web/Data/:/app/Data/

  nginx:
    volumes:
      - ./nginx/dev-certs/:/etc/letsencrypt/live/stream-schedule.io/
    ports:
      - 50080:80
      - 50443:443
